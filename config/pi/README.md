# Pi configuration

This directory contains static, non-secret Pi coding-agent configuration migrated from the Chezmoi source.

Runtime state is intentionally not managed here. Keep credentials, sessions, caches, installed extensions, and local auth outside the repository.
