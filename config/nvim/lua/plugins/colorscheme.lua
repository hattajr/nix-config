-- Activate the local "blink" colorscheme (colors/blink.lua), a port of the
-- Rainglow blink VSCode theme.
return {
  {
    "LazyVim/LazyVim",
    opts = {
      colorscheme = "blink",
    },
  },
}
