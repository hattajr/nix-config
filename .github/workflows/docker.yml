name: Docker validation

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  docker-validation:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Run ephemeral Docker validation
        run: make docker-validation
